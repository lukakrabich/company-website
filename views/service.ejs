<!DOCTYPE html>
<html lang="ru">
<head>
  <meta name="description" content="Альянс Торг – профессиональные услуги в сфере финансов, бухгалтерии и консалтинга.">
  <meta name="keywords" content="услуги, Альянс Торг, финансы, бухгалтерия, консалтинг, Москва">
  <meta name="author" content="Альянс Торг">

  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= service.name %> – Альянс Торг</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-800 font-sans">
  <header class="bg-blue-600 text-white p-4">
    <div class="container mx-auto flex justify-between items-center">
      <h1 class="text-2xl font-bold">Альянс Торг</h1>
      <nav class="space-x-4">
        <a href="/" class="hover:underline">Главная</a>
        <a href="/catalog" class="hover:underline">Каталог</a>
        <a href="/cart" class="hover:underline">Корзина</a>
        <a href="/contacts" class="hover:underline">Контакты</a>
        <a href="/account" class="hover:underline">Мои заказы</a>
      </nav>
    </div>
  </header>

  <main class="max-w-4xl mx-auto p-6 space-y-8">
    <div class="bg-white p-6 rounded shadow">
      <h2 class="text-3xl font-bold mb-2"><%= service.name %></h2>
      <p class="text-gray-600 mb-2"><%= service.type %> | <%= service.category %></p>
      <p class="text-blue-600 font-bold text-xl mb-4"><%= service.price %> ₽</p>
      <p class="mb-6"><%= service.description %></p>

      <form action="/cart/add/<%= service.id %>" method="POST">
        <button class="bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700">
          Добавить в корзину
        </button>
      </form>
    </div>

    <!-- Отзывы -->
    <section class="bg-white p-6 rounded shadow space-y-4">
      <h3 class="text-xl font-semibold mb-4">Отзывы клиентов</h3>
      <% if (serviceReviews.length === 0) { %>
        <p class="text-gray-500">Пока нет отзывов.</p>
      <% } else { %>
        <ul class="space-y-2">
          <% serviceReviews.forEach(r => { %>
            <li class="border-b pb-2">
              <strong><%= r.name %>:</strong> <%= r.text %>
            </li>
          <% }) %>
        </ul>
      <% } %>

      <!-- Форма отзыва -->
      <form action="/service/<%= service.id %>/review" method="POST" class="space-y-3 mt-4">
        <input type="text" name="name" placeholder="Ваше имя" required class="w-full border p-2 rounded">
        <textarea name="text" placeholder="Ваш отзыв" required class="w-full border p-2 rounded h-24"></textarea>
        <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">
          Оставить отзыв
        </button>
      </form>
    </section>
  </main>

  <footer class="bg-gray-200 text-center p-4 mt-10 text-sm">
    <p>© ООО «Альянс Торг», 2025. Все права защищены.</p>
  </footer>
</body>
</html>
