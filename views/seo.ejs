<!DOCTYPE html>
<html lang="ru">
<head>
  <meta name="description" content="Альянс Торг – профессиональные услуги в сфере финансов, бухгалтерии и консалтинга.">
  <meta name="keywords" content="услуги, Альянс Торг, финансы, бухгалтерия, консалтинг, Москва">
  <meta name="author" content="Альянс Торг">

  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SEO-настройки – Альянс Торг</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800">
  <header class="bg-blue-600 text-white p-4">
    <div class="container mx-auto flex justify-between items-center">
      <h1 class="text-xl font-bold">Панель SEO-настроек</h1>
      <a href="/admin" class="hover:underline">Назад в админку</a>
    </div>
  </header>

  <main class="max-w-4xl mx-auto p-6">
    <form action="/admin/seo" method="POST" class="space-y-8 bg-white p-6 rounded shadow">
      <% Object.keys(seoSettings).forEach(path => { %>
        <div class="space-y-2 border-b pb-4">
          <h2 class="text-lg font-semibold text-blue-600">Страница: <%= path %></h2>
          <input type="text" name="title_<%= path %>" value="<%= seoSettings[path]?.title || '' %>" placeholder="Title" class="w-full border p-2 rounded" required>
          <textarea name="description_<%= path %>" placeholder="Description" class="w-full border p-2 rounded h-20" required><%= seoSettings[path]?.description || '' %></textarea>
        </div>
      <% }) %>
      <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700">Сохранить</button>
    </form>
  </main>

  <footer class="bg-gray-200 text-center p-4 mt-10 text-sm">
    <p>© ООО «Альянс Торг», 2025. Все права защищены.</p>
  </footer>
</body>
</html>
