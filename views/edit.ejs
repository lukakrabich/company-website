<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Редактирование – Альянс Торг</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-800 font-sans">
  <header class="bg-blue-600 text-white p-4">
    <div class="container mx-auto flex justify-between items-center">
      <h1 class="text-2xl font-bold">Админ-панель</h1>
      <a href="/admin" class="hover:underline">← Назад к списку</a>
    </div>
  </header>

  <main class="max-w-xl mx-auto p-6 space-y-10">
    <h2 class="text-3xl font-bold text-center">Редактировать услугу</h2>

    <form action="/admin/edit/<%= service.id %>" method="POST" class="space-y-4 bg-white p-6 rounded shadow">
      <input type="text" name="name" placeholder="Название" required value="<%= service.name %>" class="w-full border p-2 rounded">
      <input type="text" name="type" placeholder="Тип" required value="<%= service.type %>" class="w-full border p-2 rounded">
      <input type="text" name="category" placeholder="Категория" required value="<%= service.category %>" class="w-full border p-2 rounded">
      <input type="number" name="price" placeholder="Цена, ₽" required value="<%= service.price %>" class="w-full border p-2 rounded">
      <textarea name="description" placeholder="Описание" required class="w-full border p-2 rounded h-28"><%= service.description %></textarea>
      <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700">Сохранить изменения</button>
    </form>
  </main>

  <footer class="bg-gray-200 text-center p-4 mt-10 text-sm">
    <p>© ООО «Альянс Торг», 2025. Все права защищены.</p>
  </footer>
</body>
</html>
